/* Acá va tu código */
window.onload = function () {
    /* Llamando al numero de intervalo, llamando al boton uno que traera el mensaje */
     
    const intervalo = document.getElementById ("intervalo").addEventListener('click', () =>{
    let mensaje_uno = document.getElementById ("cifrar").value;
    let botonouno= document.getElementById('textIn').value;
    alert (cifrar);
    document.getElementById('cajauno').innerHTML = result; })}
